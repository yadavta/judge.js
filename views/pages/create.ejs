<!DOCTYPE html>
<html>
<head>
    <%- include ("../partials/header.ejs") %>
    <script src="https://cdn.jsdelivr.net/npm/axios/dist/axios.min.js"></script>

</head>

<body>

  <%- include ("../partials/nav.ejs") %>

  <div class="ui form">
    <div class="field">
      <label>Date</label>
      <div class="ui calendar">
        <div class="ui input left icon">
          <i class="calendar icon"></i>
          <input type="text" placeholder="Pick up a date" name="date">
        </div>
      </div>
    </div>
  </div>

  <button class="ui button"> Finalize Dates </button>

  <script>

      $(function() {
        $('input[name="daterange"]').daterangepicker({
          opens: 'left'
        }, function(start, end, label) {
          console.log("A new date selection was made: " + start.format('YYYY-MM-DD') + ' to ' + end.format('YYYY-MM-DD'));
        });
      });

      /*axios
  		.post("https://judge-js.herokuapp.com/create", {
  			tournamentName: 'Berkley',
  			schoolApproved: true,
  			tabroomName: "fancy prestigious annual tournament",
  		})
  		.then(response => (this.message = response))*/

  </script>
</body>

</html>
